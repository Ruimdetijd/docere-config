import { extendConfig } from '../../index'

const config: DocereConfig = {
	slug: 'kranten1700',
	title: 'Kranten 1700',
	metadata: [],
	notes: [],
	pages: [],
	textdata: [],
}

export default extendConfig(config)
